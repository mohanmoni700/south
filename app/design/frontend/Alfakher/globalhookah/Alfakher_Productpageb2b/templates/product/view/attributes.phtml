<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */
// phpcs:disable Magento2.Templates.ThisInTemplate.FoundThis
/**
 * Product additional attributes template
 *
 * @var $block \Magento\Catalog\Block\Product\View\Attributes
 */
?>
<?php
    $_helper = $this->helper(Magento\Catalog\Helper\Output::class);
    $_product = $block->getProduct();
    $attributeCodes = [
    'vase_style_title',
    'vase_style',
    'screw_on_vase_capability_title',
    'screw_on_vase_capability',
    'multi_hose_capability_title',
    'multi_hose_capability',
    'included_accessories_title',
    'included_accessories',
    'colors_title',
    'colors',
    'hookah_brand_title',
    'hookah_brand',
    'hookah_size_title',
    'hookah_size',
    'hookah_country_of_manufacture_title',
    'hookah_country_of_manufacture'];
    $shouldDisplaySpecs = false;
    foreach ($attributeCodes as $attributeCode) {
        if ($_product->getData($attributeCode) !== null) {
            $shouldDisplaySpecs = true;
            break;
        }
    }
?>
<?php if ($shouldDisplaySpecs): ?>
<div class ="main_wrapper">
<div class="product_attributes">
<div class = "Product_attributes_head">
<span>
Al Fakher Tobacco Specs
</span>
</div>
<table id="t">
<caption></caption>
  <tbody>
    <th></th>
    <th></th>
  <?php foreach ($attributeCodes as $attributeCode): ?>
    <?php if ($attributeValue = $_product->getData($attributeCode) !== null) :?>
    <tr>
      <td class="td_first">
      <?= /* @noEscape */ str_replace(['_', 'product'], [' ', ''], $attributeCode); ?>
      <td>
        <?php $attributeValue = $_product->getData($attributeCode); ?>
        <?php if ($attributeValue !== null && $attributeValue !== '') :?>
            <?= /* @noEscape */  $attributeValue; ?>
        </td>
        <?php endif; ?>
    </tr>
    <?php endif; ?>
    <?php endforeach; ?>
  </tbody>
</table>
</div>
</div>
<?php endif; ?>

